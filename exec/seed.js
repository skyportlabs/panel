const { db } = require('../handlers/db');
const axios = require('axios');

// skyport/panel/exec/seed.js

// TODO:
// Make the Panel get all of the Images from the Skyport repo
// Then store them in db with their info
// There should be a setup command as well I suppose (maybe)

// - This will be in the official docs for during the setup process

console.log('did shit')
process.exit();